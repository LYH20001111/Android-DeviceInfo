- 依赖与插件版本统一收敛到 gradle/libs.versions.toml，并通过 alias(libs.*) 在模块中引用，避免硬编码版本号
- 构建脚本使用 Kotlin DSL（*.gradle.kts），并在根工程通过 dependencyResolutionManagement 强制集中仓库管理（FAIL_ON_PROJECT_REPOS）
- Android 模块通过 namespace 与 applicationId 分离包名与安装标识，默认配置集中声明 minSdk/targetSdk/versionCode/versionName